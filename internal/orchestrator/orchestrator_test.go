// Package orchestrator_test contains tests for the Orchestrator FSM.
//
// The legacy ExperimentRun-shaped integration tests were dropped during the
// migration #19 phase-first refactor; the phase-aware orchestrator (and its
// tests) is a follow-up. This file keeps the package compilable.
package orchestrator
